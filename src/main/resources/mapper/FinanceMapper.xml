<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shadowkylin.ham.dao.FinanceDao">
    <select id="getFinanceList" resultType="List">
        SELECT *
        FROM finance
        where user_id = #{user_id}
    </select>
    <select id="getFinanceDetail" resultType="Finance">
        SELECT *
        FROM finance
        WHERE id = #{finance_id}
    </select>
    <insert id="addFinance">
        INSERT INTO VALUES (#{asset_type_id}, #{user_id}, #{date}, #{type}, #{amount}, #{notes})
    </insert>
    <update id="updateFinance">
        UPDATE finance
        SET asset_type_id = #{asset_type_id},
            user_id       = #{user_id},
            date          = #{date},
            type          = #{type},
            amount        = #{amount},
            notes         = #{notes}
        WHERE id = #{id}
    </update>
    <delete id="deleteFinance">
        DELETE
        FROM finance
        WHERE id = #{id}
    </delete>
    <delete id="deleteFinanceList">
        DELETE
        FROM finance
        WHERE id IN
        <foreach collection="idList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>