<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.shadowkylin.ham.dao.AccountDao">
    <select id="getAccountList" resultType="List">
        SELECT *
        FROM account
    </select>
    <select id="getAccountDetail" resultType="User">
        SELECT *
        FROM account
        WHERE id = #{id}
    </select>
    <insert id="addAccount">
        INSERT INTO account (username, password, phone, created_time)
        VALUES ( #{username}, #{password}, #{phone}, #{createdTime})
    </insert>
    <update id="updateAccount">
        UPDATE account
        SET username = #{username},
            password = #{password},
            phone    = #{phone} ,
            createdTime = #{createdTime}
        WHERE id = #{id}
    </update>
    <delete id="deleteAccount">
        DELETE FROM account
        WHERE id = #{id}
    </delete>
    <delete id="deleteAccountList">
        DELETE FROM account
        WHERE id IN
        <foreach collection="idList" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>